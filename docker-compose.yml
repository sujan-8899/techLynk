# Copy .env.example to .env and set RESEND_API_KEY so contact/journey forms work.
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
